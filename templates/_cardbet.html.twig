<!-- CARD MATCH START -->

        <div class="card h-100 mt-3 mx-2" style="width: 25rem;">
            <ul class="list-group list-group-flush">
                <li class="text-center" style="list-style-type: none;">

                    <!-- CHECK BOX SELECTION MATCH START -->
                    {% if app.user %}
                        <div class="checkbox-container">
                            <input type="checkbox" id="{{ footballMatch.id }}" class="large-checkbox mt-3 mx-2" name="selectedMatches[]" value="{{ footballMatch.id }}">
                            <label for="{{ footballMatch.id }}">
                                Sélectionner Le Match
                            </label>
                        </div>
                    {% endif %}
                    <!-- CHECK BOX SELECTION MATCH END -->


                    <!-- DATE MATCH START -->
                         <div class="text-center mt-2"> Le {{footballMatch.matchDate|date('d/m/Y')}} </div>
                    <!-- DATE MATCH END -->

                    <!-- HOUR MATCH START -->
                        <div class="text-center mt-2">
                            Début {{footballMatch.hourStart|date('H:i')}}
                            Fin {{footballMatch.hourFinish|date('H:i')}}
                        </div>
                    <!-- HOUR MATCH END -->

                    <!-- STATUS MATCH  -->
                        <div class="match-status text-center text-danger mt-2 mb-2">{{footballMatch.statut}} </div>
                    <!-- Black Line -->
                        <hr style="background-color: black; height: 1px; border: none; margin-top: 0;">
                </li>

                <!-- TEAM 1 & TEAM 2 LOGO AND NAME START -->
                <div class="row mt-2 mb-2 text-center">

                        <!-- Column Logo team 1 -->
                        <div class="col">
                            <img src="{{ asset('img/teams/' ~ footballMatch.team1.link) }}" class="imgteam" alt="logo">
                        </div>

                        <!-- Column team 1 Name -->
                        <div class="col d-flex justify-content-center align-items-center">
                            {{ footballMatch.team1.teamName }}
                        </div>

                        <!--  VS LOGO START -->
                        <div class=" text-center mt-2 mb-2">
                            <img src="{{ asset('img/teams/vs.png') }}" class="imgteam" alt="logo">
                        </div>
                        <!--  VS LOGO END -->

                        <!-- Column Logo team 2 -->
                        <div class="col">
                            <img src="{{ asset('img/teams/' ~ footballMatch.team2.link) }}" class="imgteam" alt="logo">
                        </div>

                        <!-- Column team 2 Name -->
                        <div class="col d-flex justify-content-center align-items-center">
                            {{ footballMatch.team2.teamName }}
                        </div>

                </div>

                <!-- TEAM 1 & TEAM 2 LOGO AND NAME END -->

                <!-- BUTTON MISER SUR LA SELECTION START -->
                {% if app.user %}
                    <a href="{{ path('miser', { 'footballMatch' : footballMatch.id }) }}" class="card-link text-center btn btn-danger mt-2 mb-2">Miser sur la sélection</a>
                {% endif %}
                <!-- BUTTON MISER SUR LA SELECTION END -->
            </ul>

        </div>
        <!-- CARD MATCH END-->