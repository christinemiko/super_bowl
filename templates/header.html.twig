<!-- NAV BAR START -->
<header class="transparent-header">

    <nav class="navbar navbar-expand-lg navbar-lig custom-navbar">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('accueil') }}"><img src="{{ asset('img/logo4.png') }}" alt="Logo super_bowl2024" width="80" height="80"></a>
            <button class="navbar-toggler navbar-dark" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon "></span>
            </button>
            <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
                <ul class="navbar-nav">

                    <li class="nav-item">
                        <a class="nav-link nav-link-home" aria-current="page" href="{{ path('accueil') }}">Accueil</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('visualiserlesmatchs') }}" >
                           Visualiser Les Matchs</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('parier') }}" >
                         Parier</a>
                    </li>
                    {% if app.user %}
                    {% if is_granted("ROLE_ADMIN") %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin') }}" >
                         Administration</a>
                    </li>

                    {% else %}

                {% endif %}

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Mon Espace
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item text-warning" href="{{ path('myaccount') }}">Mon Espace</a></li>
                            <li><a class="dropdown-item text-danger" href="{{ path('history') }}">Historique des Mises</a></li>
                            <li><a class="dropdown-item text-primary" href="{{ path('information') }}">Informations personnelles</a></li>
                        </ul>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}" >
                          Déconnexion</a>
                    </li>


                {% else %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}" >
                           Se Connecter</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('inscription') }}" >
                           S'Inscrire</a>
                    </li>

                {% endif %}

                </ul>
            </div>
        </div>
    </nav>
</header>

<!-- NAV BAR END -->